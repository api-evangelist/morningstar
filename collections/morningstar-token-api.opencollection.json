{
  "opencollection": "1.0.0",
  "info": {
    "name": "Authorization Tokens Accounts Token API",
    "version": "1.0.0"
  },
  "request": {
    "auth": {
      "type": "basic",
      "username": "{{username}}",
      "password": "{{password}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "Token",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Generate a JSON Web Token (JWT)",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://www.us-api.morningstar.com/token/token/oauth"
          },
          "docs": "Authenticates a client using HTTP Basic Auth and returns a signed JWT\nbearer token for use in subsequent API requests.\n\nPass your credentials as a Base64-encoded `username:password` string in\nthe `Authorization` header. The returned `access_token` should be included\nas a `Bearer` token in the `Authorization` header of all authenticated requests."
        }
      ]
    }
  ],
  "bundled": true
}